C/C++里面我容易犯错的毛病

1.有时候for循环里面的东西需要在每次进入for循环前再重新赋值 ==这点我经常忘记==

```c
int sum = 0;
for(int i=0;i < n;i++ )
{
    sum = 0;//这一步容易忘记
    for(int j=0;j<n;j++)
    {
        scanf("%d", &a[i][j]);
        sum+=a[i][j];
    }
    printf("%d\n", a[i][j]);
}
```

2.==if和else== if多个if语句 若一个语句里面改变的值会影响到下一个if语句的条件判断 需要考虑要不要换成else if ==经常犯错==

```c
if(n>0 && s[i]+n<='z')//若全写if就会是错误的
    s[i] += n;
else if(n>0 && s[i]+n>'z')
    s[i] -= 26-n;
else if(n<0 && s[i]+n>='a')
    s[i] += n;
else if(n<0 && s[i]+n<'a')
    s[i] += (26+n);
```

3.结构体的传参是箭头==->==还是==.== 这两个操作符要区分清楚

```c
//直接引用
typedef struct
{
  int a;
  char b;
  float c;
}Stu;
int main()
{
    Stu arr[10];
    return 0;
}
```

```c++
//间接引用
struct student
{
	string name;  //姓名
	int age;      //年龄
	int score;    //分数
};
int main() 
{
	struct student stu = { "张三",18,100, };
	struct student * p = &stu;
	p->score = 80; //指针通过 -> 操作符可以访问成员
	cout << "姓名：" << p->name << " 年龄：" << p->age << " 分数：" << p->score << endl;
	system("pause");
	return 0;
}
```

4.容易忘记==初始化==

```c++
int main()
{
 long long m,k;
 int sum = 0;//这里的sum要是写在long long那一行 我就可能会忘记初始化
 cin >> m >> k;
 while (m)
 {
 if (m % 10 == 3)
 sum++;
 m /= 10;
 }
 if(sum == k)
 cout << "YES" << endl;
 else
 cout << "NO" << endl;
 
 return 0;
}
```

5.有时候甚至忘记输入==cin==

```c++
int main()
{
    long long x,sum=0;//这里sum记得初始化为0
    int n;
    //cin >> n; //这里的cin我有时候就会忘记
    for(int i = 0;i < n;i++)
    {
        cin >> x;
	}
    //......
	return 0;
}
```

6.有时候老错记得再看看题目 不要拿到题目没看仔细 就吭哧吭哧地开始敲代码了
